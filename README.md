# E-Commerce Frontend

Initial frontend scaffold for the e-commerce project.

Files created:
- `index.html`
- `styles/main.css`
- `scripts/app.js`

Run `npm init -y` to create `package.json` (already scripted in setup steps).
